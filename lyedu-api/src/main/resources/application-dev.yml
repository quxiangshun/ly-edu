# 本地 Debug：配合 compose-mysql-redis.yml，连接本机 3306/6379
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/lyedu?useUnicode=true&characterEncoding=UTF-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true
    username: root
    password: lyedu123456
  data:
    redis:
      host: localhost
      port: 6379

logging:
  level:
    com.lyedu: debug
