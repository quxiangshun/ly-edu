```shell
 # 停止并清理
docker compose -f compose-mysql-redis.yml down -v
# 重新启动
docker compose -f compose-mysql-redis.yml up -d
# 启动 Python 项目（会自动执行迁移）
.\venv\Scripts\python.exe -m uvicorn main:app --host 0.0.0.0 --port 9700


# 查看97000端口占用清空
netstat -ano | findstr :9700
# 杀掉进程
taskkill /PID 13412 /F
```