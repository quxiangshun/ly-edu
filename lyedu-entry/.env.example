# 统一入口跳转地址（构建时注入；同域部署可用相对路径，跨域填完整 URL）

# PC 端地址（默认 /pc/）
VITE_PC_URL=/pc/

# H5 端地址（默认 /h5/）
VITE_H5_URL=/h5/

# 管理后台地址（默认 /admin/）
VITE_ADMIN_URL=/admin/

# 是否根据设备自动跳转：true=手机跳 H5、电脑跳 PC；不设或 false=仅展示选择页
VITE_AUTO_REDIRECT=true
