# Docker Compose 使用（本目录下执行 docker compose 时自动加载 .env）
# 复制为 .env 后按需修改

# 镜像前缀（国内可填 registry 地址，如 registry.cn-hangzhou.aliyuncs.com/）
DOCKER_REGISTRY=

# 前端构建时的 npm 源（国内可填 https://registry.npmmirror.com）
NPM_REGISTRY=https://registry.npmmirror.com
